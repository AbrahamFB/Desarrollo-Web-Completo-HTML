//  Objeto Math

window.Math.random();
Math.random();

resultado = Math.PI;
resultado = Math.round(2.5);
resultado = Math.ceil(2.1); //Siempre redondea hacia arriba
resultado = Math.floor(2.9); //Siempre redondea hacia abajo
resultado = Math.sqrt(144); //Raíz cuadrada
resultado = Math.abs(-100); //Valor absoluto
resultado = Math.min(3, 5, 7, 1, 7, 9, 52, 74, 1, 55, 0, 4); //Valor menor de una lista
resultado = Math.max(3, 5, 7, 1, 7, 9, 52, 74, 1, 55, 0, 4); //Valor mayor de una lista
resultado = Math.random(); //Números aleatorios

console.log(resultado);
