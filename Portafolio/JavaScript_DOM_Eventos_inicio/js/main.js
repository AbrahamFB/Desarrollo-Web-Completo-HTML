// querySelector - Retorna ya sea ningun o solo uno que concuerde con el selector
const heading = document.querySelector(".header__texto h2"); //Retorna 0 o 1 Elemento
console.log(heading);

// querySelectorAll

// querySelectorAll
